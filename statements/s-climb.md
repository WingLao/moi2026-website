# 爬上爬落 (climb)

> 此網站版本改用 Markdown 題面，範例輸入 / 輸出直接對齊 `data` 內的 `*-0.in` / `*-0.out`，避免題面與測資不一致。

| | |
|----|---|
|任務名稱|climb|
|組別|S|
|時限|0.5秒|
|測試數據數目|10|
|記憶限制| 128M |
|測資目錄|`data/S/climb`|
|原始 PDF|`S-climb.pdf`|
|對齊範例|`climb-0.in` / `climb-0.out`|

## 題目前置說明

若要編譯, 在終端機上輸入以下指令, 其他的也是如是
```
compile  climb.cpp
run . climb.cpp
如跑 Python 的話, 直接打:
run . climb.py
假設你的文件在一個文件夾內 (如 balls), 則先要進入你的文件夾, 才可編譯:
cd ~/balls
compile balls.cpp
n
f(x) = anxn + an−1xn−1 + an−2xn−2 + ⋯+ a1x + a0
2 ≤n ≤8
n −1
給定一個  次函數
其中
。且這個函
數共有
個極值 (這些極值可以想像為在函數曲線內出現的一個個山峰頂或山谷底)。
給定一個 $n$ 次函數 $f(x)=a_n x^n+a\sub{n-1}x^{n-1}+a\sub{n-2}x^{n-2}+\dots+a_1x+a_0$ 其中
$2\le n \le 8$。且這個函數共有 $n-1$ 個極值 (這些極值可以想像為在函數曲線內出現的一個個山
峰頂或山谷底)。
給定
及
且在這個區間內，
只有一個極值 (山峰頂或山谷底)。我們需要找出在這區間內
的極值點
(即山峰頂或山谷底所在的  位置)。
x1
x2
f(x)
x0
x

## 輸入格式

給定 $x_1$ 及 $x_2$ 且在這個區間內，$f(x)$ 只有一個極值 (山峰頂或山谷底)。我們需要找出在
這區間內的極值點 $x_0$ (即山峰頂或山谷底所在的 $x$ 位置)。

輸入數據的第一行上有一個正整 $T$，代表共有多少組測試數據。

隨後的  組數據，每組的格式如下: (
)

第一行上有三個數
,
, 及
, 其中
第二行上有
個實數，每個實數小數點後最多有 4 個位。 這
個實數順次分別代
表着
次函數的系數

N X1
X2
X1 < X2
N + 1
N + 1
N
aN, aN−1, aN−2, … , a1, a0

隨後的 $T$ 組數據，每組的格式如下: ($1\le T \le 10$)
- 第一行上有三個數 $N$, $X1$, 及 $X2$, 其中 $X1< X2$
- 第二行上有 $N+1$ 個實數，每個實數小數點後最多有 4 個位。 這 $N+1$ 個實數順次分別代表
着 $N$ 次函數的系數 $a_N, a\sub{N-1}, a\sub{N-2}, \dots, a_1, a_0$

## 輸出格式

輸出一個實數代表你所找到的極值所在的  (即
是在
及
區間內的極值)。輸出的實數
要求小數點後必須顯示正好 5 個位

x
f(x)
X1
X2

## 範例輸入

```text
1
2 -2.5 1.5
1.0 1.0 -6.0
```

## 範例輸出

```text
-0.50000
```

## 特殊條件

-0.50000
```

- 有一個測試數據 $N=2$
- 有一個測試數據 $N=3$

N = 2
N = 3
