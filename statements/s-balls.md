# 收拾球子 (balls)

> 此網站版本改用 Markdown 題面，範例輸入 / 輸出直接對齊 `data` 內的 `*-0.in` / `*-0.out`，避免題面與測資不一致。

| | |
|----|---|
|任務名稱|balls|
|組別|S|
|時限|0.5秒|
|測試數據數目|10 (但暫時系統內只有4個)|
|記憶限制| 256M |
|測資目錄|`data/S/balls`|
|原始 PDF|`S-balls.pdf`|
|對齊範例|`balls-0.in` / `balls-0.out`|

## 題目前置說明

數據在搬運期中可能有誤! 稍後會再測試一次!

## 題目說明

數據在搬運期中可能有誤! 稍後會再測試一次!
小明在拾一些球子，他共有 $N$ 個完全相同的球子，另外他有 $K$ 個容量不同且排剄列在一櫃
子內的盒子。
這些盒子分別以 1 至 $K$ 加以編號。已知龠子的總容量是大於等於球的數目。
小明想知道總共有多少種方法，可以把這 $N$ 個球子分配放到不同的盒子裡去。
小明在拾一些球子，他共有
個完全相同的球子，另外他有
個容量不同且排剄列在一櫃子
內的盒子。 這些盒子分別以 1 至
加以編號。已知龠子的總容量是大於等於球的數目。 小明
想知道總共有多少種方法，可以把這
個球子分配放到不同的盒子裡去。

## 輸入格式

輸入的第一行有一個整數  (
)，代表輸人資料含有多少組測試數據。

輸入的第一行有一個整數 $T$ ($1\le T \le 20$)，代表輸人資料含有多少組測試數據。

每一組測試數據的格式如下:

每一組測試數據的格式如下:
- 第一行有兩個正整 $N$ 及 $K$  ($N\le200$)
- 第二行上有 $K$ 個正整數，這些正整數順序表示若一個盒的容量。($1\le K\le 10$)

第一行有兩個正整
及
(
)
第二行上有
個正整數，這些正整數順序表示若一個盒的容量。(
)

## 輸出格式

對應於每一組輸入資料，輸出一個正整數，代表你找到的可能放置方法數目。 因為這個數字會
很大，所以只需輸出數目 MOD 100,000,007 即可

## 範例輸入

```text
1
6 3
2 1 5
```

## 範例輸出

```text
5
```

## 特殊條件

最少有一個測數據內的所有盒子容量是全部相同的。
